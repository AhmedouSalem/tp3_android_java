<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tp3_android_persistence" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary branding color. -->
        <item name="colorPrimary">@color/primaryBackground</item>
        <!-- Secondary branding color. -->
        <item name="colorSecondary">@color/white</item>

        <!-- Police par défaut -->
        <item name="android:fontFamily">@font/tinos</item>
        <item name="fontFamily">@font/tinos</item>
    </style>

    <style name="Theme.Tp3_android_persistence" parent="Base.Theme.Tp3_android_persistence" />
</resources>